name: kouroshyar
description: KouroshYar - secure legal assistant Android build package.
publish_to: "none"
version: 3.5.0+61

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  path_provider: ^2.1.3
  path: ^1.9.0
  http: ^0.13.6
  speech_to_text: ^7.0.0
  image_picker: ^1.1.2
  file_picker: ^8.1.7
  local_auth: ^2.3.0
  crypto: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
