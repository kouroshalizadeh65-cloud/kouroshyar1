# تغییرات کوروش‌یار v3.6.52+124

## اصلاح Build

- import اضافی `dart:typed_data` از `lib/database/app_database.dart` حذف شد.
- این اصلاح خطای `unnecessary_import` گزارش‌شده در مرحله `flutter analyze` را برطرف می‌کند.
- هیچ منطق اجرایی، قابلیت، ساختار داده، پشتیبان یا تنظیم امنیتی تغییر نکرده است.

## نسخه و خروجی

- نسخه برنامه به `3.6.52+124` افزایش یافت.
- نام APK و Artifact در GitHub Actions به نسخه `3.6.52` به‌روزرسانی شد.
