# گزارش بسته سورس کوروش‌یار 3.6.59+131

## هویت بسته

- نسخه برنامه: `3.6.59+131`
- Package ID: `com.example.kouroshyar`
- نسخه ربات: `1.3.0`
- نسخه طبقه‌بندی: `1.3.0-county-scope`
- کف بازبینی تعطیلات: `4`
- کف بازبینی ساعات کاری: `4`
- مبنا: `kouroshyar_v3_6_58.zip`
- SHA-256 مبنا: `dfc2c2694464f61901814b6b331214d208ee25e635cafa558c3d5e446537d662`

## تغییرات اصلی کنترل‌شده

- مدل تعطیلات و ساعات کاری از محدوده `county`، فهرست `counties` و فهرست `excludedCounties` پشتیبانی می‌کند.
- تعطیلی شهرستانی به‌عنوان تعطیلی کل استان محاسبه نمی‌شود.
- تقویم ماهانه و هفتگی تعطیلی محلی را جداگانه علامت می‌زند و صفحه روز جزئیات محدوده و منبع را نمایش می‌دهد.
- کارت‌های کاهش ساعت و دورکاری نام شهرستان‌ها یا شهرستان‌های مستثنا را نمایش می‌دهند.
- طبقه‌بند ربات اطلاعیه‌های چندبخشی و چندگروهی شهرستان‌ها را به رکوردهای مستقل تبدیل می‌کند.
- همه ۳۱ خوراک استانی پیکربندی شده‌اند.
- نام APK و Artifact در GitHub Actions از نسخه واقعی `pubspec.yaml` ساخته می‌شود.

## نتایج بررسی محلی

- تست‌های Python: **۲۵ تست موفق**.
- Bot Doctor: **موفق**؛ ۳۶ منبع، ۳۵ منبع در حالت جاری و ۴ منبع در حالت آرشیوی.
- کنترل‌های ایستا: **۲۸ از ۲۸ موفق**.
- Parse فایل‌های Python، JSON و YAML: موفق.
- کنترل مسیرهای import محلی Dart: موفق.
- کنترل تعادل جداکننده‌های فایل‌های Dart تغییرکرده: موفق.
- حذف فایل نسبت به بسته کامل 3.6.58: انجام نشده است.
- فایل حساس یا خروجی باینری داخل سورس: یافت نشد.

## مواردی که عمداً داخل بسته نیستند

- keystore و گواهی خصوصی امضای Android
- `key.properties`
- کلید خصوصی Ed25519 خوراک
- APK یا AAB
- `pubspec.lock`؛ این فایل فقط با اجرای واقعی `flutter pub get` در GitHub Actions ساخته می‌شود.
- فایل زنده امضاشده `holiday_feed/holidays.json` و `holiday_feed/working_hours.json`؛ این دو فایل با Secret موجود در GitHub و پس از اجرای موفق ربات منتشر می‌شوند و فایل‌های فعلی مخزن هنگام انتقال کنترل‌شده نباید حذف شوند.

## وضعیت تایید نهایی

این ZIP یک **بسته سورس آماده ارسال به GitHub** است، نه نسخه نهایی تاییدشده روی گوشی. Flutter SDK در محیط تولید بسته موجود نبود؛ بنابراین هنوز باید مراحل زیر در GitHub Actions و گوشی انجام شوند:

1. `flutter test`
2. `flutter analyze`
3. `flutter build apk --release`
4. بررسی Package ID، نسخه و امضای APK
5. نصب روی `3.6.58+130` بدون حذف برنامه و کنترل حفظ داده‌ها
6. دریافت خوراک امضاشده جدید با بازبینی بالاتر از ۴ و آزمون نمایش تصمیم‌های شهرستانی
