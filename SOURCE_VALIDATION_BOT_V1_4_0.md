# گزارش اعتبارسنجی سورس ربات کوروش‌یار v1.4.0

## مبنا

- سورس برنامه: `3.6.59+131`
- نسخه ربات پیشین: `1.3.2`
- نسخه ربات این بسته: `1.4.0`
- Package ID: `com.example.kouroshyar`
- کف زنده تعطیلات هنگام دریافت مبنا: `7`
- کف زنده ساعات کاری هنگام دریافت مبنا: `10`

## تغییرات محدود به ربات

- پایش مستقیم کانال عمومی رسمی استانداری ایلام در ایتا.
- جستجوی تکمیلی خودکار برای همه ۳۱ استان.
- بازبینی ۱۴ روز گذشته و پردازش مجدد هر نشانی حداکثر پس از ۲۴ ساعت.
- تشخیص تغییر متن اطلاعیه با هش محتوا.
- دریافت هم‌زمان منابع با ۸ Worker برای جلوگیری از طولانی‌شدن Workflow.
- کنترل سلامت اجباری منابع و توقف انتشار خوراک ناقص.
- گزارش سلامت نسخه دوم با پوشش استانی و منابع حیاتی ناموفق.

## کنترل‌های محلی انجام‌شده

- `python -m compileall`: موفق
- `python -m holiday_bot doctor`: موفق
- آزمون‌های Python: `41 passed`
- اعتبارسنجی امضای خوراک فعلی: موفق
- تعطیلات رسمی: ۲۶ تاریخ
- کل رکوردهای تعطیلات فعلی: ۳۵
- رکوردهای ساعات کاری فعلی: ۱۵
- رکورد بازه‌ای ساعات کاری: ۱
- رکوردهای موردی ساعات کاری: ۱۴
- پوشش جستجوی تکمیلی: ۳۱ استان
- منابع پیکربندی‌شده: ۶۹
- منابع اجرای جاری: ۶۸

## کنترل عدم تغییر برنامه

مسیرهای `lib/`، `android/`، `test/`، `assets/` و فایل `pubspec.yaml` نسبت به ZIP دریافت‌شده از شاخه اصلی، بایت‌به‌بایت بدون تغییر باقی مانده‌اند.

## کنترل امنیت

- keystore وجود ندارد.
- `key.properties` وجود ندارد.
- فایل PEM یا کلید خصوصی وجود ندارد.
- APK وجود ندارد.
- `pubspec.lock` وجود ندارد.
- کلید خصوصی خوراک همچنان فقط از Secret با نام `HOLIDAY_FEED_PRIVATE_KEY_B64` دریافت می‌شود.

## وضعیت تایید نهایی

اجرای زنده GitHub Actions، انتشار خوراک جدید و دریافت آن روی گوشی در محیط محلی قابل انجام نبود. موفقیت نهایی ربات فقط پس از Push کنترل‌شده، سبزشدن Workflow، مشاهده گزارش سلامت و دریافت خوراک جدید روی گوشی قابل اعلام است.
