# اعتبارسنجی سورس نسخه 3.6.53+125

## کنترل‌های انجام‌شده در محیط تولید ZIP

- توسعه از ZIP رسمی v3.6.52+124 با SHA-256 ثبت‌شده انجام شد.
- Package ID بدون تغییر و برابر `com.example.kouroshyar` باقی ماند.
- پوشه کامل Android حفظ شد.
- هیچ فایل `*.jks`، `*.keystore` یا `android/key.properties` افزوده نشد.
- Secretهای امضای APK و Alias تغییر نکردند.
- صفحه پیش‌فرض، نوار چهارگانه، تقویم شمسی و ساختار داده‌های قبلی حفظ شدند.
- فایل‌های قدیمی و قابلیت‌های موجود حذف نشدند.

## محدودیت این محیط

Flutter و Dart SDK در محیط تولید ZIP نصب نبودند؛ بنابراین `flutter pub get`، `flutter test`، `flutter analyze` و `flutter build apk` در این محیط اجرا نشده‌اند. `pubspec.lock` به‌صورت دستی ساخته نشده و Workflow موظف است آن را با اجرای واقعی `flutter pub get` تولید کند.

موفقیت نهایی فقط پس از اجرای کامل GitHub Actions، بررسی امضا و Package ID و نصب روی گوشی اعلام می‌شود. Workflow فایل `pubspec.lock` تولیدشده در همان اجرای واقعی را نیز کنار گزارش‌های Build در Artifact قرار می‌دهد تا برای تثبیت سورس بعدی قابل دریافت باشد.
