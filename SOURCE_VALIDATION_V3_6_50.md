# گزارش اعتبارسنجی سورس v3.6.50+122

## مبنا

- سورس مبنا: `kouroshyar_v3_6_49_notifications_search_ui_hotfix_1405_04_25.zip`
- نسخه خروجی: `3.6.50+122`
- دامنه: تفکیک مهلت شخصی از کار و یادآوری، مهاجرت داده و اعلان اختصاصی.

## کنترل‌های انجام‌شده

- تمام فایل‌های Dart پوشه‌های `lib` و `test` با parser زبان Dart بررسی شدند و خطای نحوی یافت نشد.
- importهای نسبی داخلی بررسی شدند.
- فایل‌های YAML و XML از نظر ساختاری بررسی شدند.
- مدل مهلت قدیمی بدون فیلد جدید با مقدار پیش‌فرض سازگار خوانده می‌شود.
- شناسه بسته Android و تنظیمات امضای دائمی تغییر نکرده است.
- keystore، رمز امضا و `key.properties` داخل سورس قرار نگرفته‌اند.

## محدودیت اعتبارسنجی

Flutter SDK در محیط تولید این ZIP نصب نیست؛ بنابراین اجرای واقعی `flutter test`، `flutter analyze` و `flutter build apk --release` انجام نشده است. موفقیت نهایی باید در GitHub Actions تأیید شود.
