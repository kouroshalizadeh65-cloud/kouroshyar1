# گزارش اعتبارسنجی سورس v3.6.54+126

## بررسی ایستا

- توسعه مستقیم از ZIP رسمی `v3.6.53+125` انجام شده است.
- نسخه `3.6.54+126` در `pubspec.yaml` ثبت شده است.
- Package ID همچنان `com.example.kouroshyar` است.
- هیچ فایل قبلی، صفحه، داده یا قابلیت موجود حذف نشده است.
- هیچ فایل `jks`، `keystore`، `key.properties`، PEM یا کلید خصوصی داخل سورس قرار ندارد.
- URL ساعات کاری به‌طور پیش‌فرض از مسیر همان خوراک تعطیلات و نام `working_hours.json` استخراج می‌شود.
- خوراک ساعات کاری با Ed25519 و همان کلید عمومی نسخه نصب‌شده بررسی می‌شود.
- تغییر ساعات کاری در منطق تعطیلی و شمارش روزهای تعطیل وارد نمی‌شود.

## اعتبارسنجی لازم در GitHub Actions

این محیط Flutter/Dart در اختیار نداشت؛ بنابراین موفقیت نهایی فقط پس از اجرای واقعی موارد زیر اعلام می‌شود:

```text
flutter pub get
flutter test
flutter analyze
flutter build apk --release
apksigner verify
بررسی Package ID
```

پس از Build، APK باید روی `v3.6.53+125` نصب و حفظ اطلاعات، دریافت `working_hours.json`، علامت ساعت و جزئیات روز روی گوشی آزمایش شود.
