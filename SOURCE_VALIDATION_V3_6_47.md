# اعتبارسنجی سورس v3.6.47+119

- مبنا: `v3.6.46+118`.
- هفت مورد `const [FilteringTextInputFormatter.digitsOnly]`: همگی اصلاح شدند.
- ارجاع به تابع تعریف‌نشده `_dominantFinanceType`: حذف شد.
- import اضافی `package:cross_file/cross_file.dart`: حذف شد.
- نسخه `pubspec.yaml`: `3.6.47+119`.
- نام APK و Artifact گردش‌کار GitHub Actions: به‌روزرسانی شد.
- فایل‌های Dart از نظر توازن ساختاری، مسیر importهای نسبی، XML و YAML به‌صورت محلی کنترل شدند.
- Flutter/Dart در محیط بسته‌بندی محلی نصب نیست؛ تأیید نهایی `flutter test`، `flutter analyze` و `flutter build apk --release` باید در GitHub Actions انجام شود.
