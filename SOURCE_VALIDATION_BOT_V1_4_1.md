# گزارش اعتبارسنجی سورس ربات کوروش‌یار v1.4.1 — بازبینی بسته ۲

## مبنا

- ZIP مبنا: `kouroshyar1-main.zip`
- SHA-256 مبنا: `2c33b28f1bcd60144720d14567a850bf17a72641c081a03c94b2b0bad91a3f49`
- نسخه برنامه: `3.6.59+131`
- نسخه ربات پایدار مبنا: `1.3.2`
- نسخه ربات این بسته: `1.4.1`
- Package ID: `com.example.kouroshyar`
- کف تعطیلات: `7`
- کف ساعات کاری: `10`

## علت اصلاح

Artifact اجرای ناموفق ربات 1.4.0 با SHA-256 برابر `04024c88058845414db1d6e2925f770505c06ac6d560662c66aedc89e74438ba` بررسی شد. اجرای واقعی ۶۸ منبع را آزمود، ۶۷ منبع موفق بودند و تنها منبع حیاتی «استانداری ایلام - کانال رسمی ایتا» هنگام پیمایش صفحه‌بندی با پاسخ HTTP 429 متوقف شد. Health Gate به‌درستی مانع انتشار خوراک ناقص شد.

## اصلاحات 1.4.1

- Retry محدود برای خطاهای شبکه، HTTP 5xx و 429.
- Backoff افزایشی با سقف انتظار و رعایت هدر `Retry-After`.
- تفکیک خطای موقت (`deferred`) از خطای قطعی.
- وضعیت‌های صریح `deferred_no_publish`، `failed_no_publish` و `passed`.
- حفظ قاعده fail-closed؛ هیچ اختلال منبع حیاتی به انتشار خوراک ناقص منجر نمی‌شود.
- محدودکردن اجرای جاری کانال ایتا به ۶ صفحه؛ پیمایش ۲۴صفحه‌ای فقط در `backfill_1405`.
- تأخیر کنترل‌شده میان صفحات کانال عمومی.
- ارتقای گزارش سلامت به `kouroshyar-source-health-v3` با آمار Retry و زمان انتظار.
- تولید قطعی `reports/workflow_result.json` حتی هنگام Health Gate ناموفق یا Exception.
- رفع هشدار BeautifulSoup برای متن ساده شبیه نام فایل.
- اصلاح تست revision ساعات کاری با کف زنده ۱۰.
- ارتقای Actionهای Workflow ربات به نسخه‌های Node.js 24.
- اصلاح Checkout اجرای دستی تا Branch انتخاب‌شده واقعاً آزمایش شود؛ حذف `ref: main` از مرحله Checkout.
- افزودن محافظ انتشار: اجرای `dry_run=false` خارج از Branch اصلی فوراً متوقف می‌شود.

## کنترل‌های محلی انجام‌شده

- `python -m compileall -q holiday_bot tests`: موفق
- `python -m holiday_bot doctor`: موفق
- آزمون‌های Python: `46 passed`
- `python -m holiday_bot validate`: موفق
- تعطیلات: ۳۵ رکورد، شامل ۲۶ تاریخ رسمی
- ساعات کاری: ۱۵ رکورد؛ ۱ بازه دوره‌ای و ۱۴ رویداد موردی
- پوشش جستجوی تکمیلی: ۳۱ استان
- منابع پیکربندی‌شده: ۶۹
- منابع حالت جاری: ۶۸
- آزمون Retry 429 و `Retry-After`: موفق
- آزمون اتمام Retry و طبقه‌بندی `rate_limited`: موفق
- آزمون تفکیک `deferred_no_publish`: موفق
- آزمون محدودیت صفحه جاری و Backfill: موفق
- آزمون تولید JSON شکست CLI: موفق

## کنترل عدم تغییر Flutter/Android

هش تجمعی مسیرهای زیر با ZIP مبنا مقایسه شد و بدون تغییر بود:

- `pubspec.yaml`
- `analysis_options.yaml`
- `lib/`
- `android/`
- `test/`

بنابراین نسخه برنامه همچنان `3.6.59+131` است و Package ID همچنان `com.example.kouroshyar` باقی مانده است.

## کنترل داده و امضا

- بازبینی Payload تعطیلات: `7`
- بازبینی Payload ساعات کاری: `10`
- کف بازبینی تعطیلات: `7`
- کف بازبینی ساعات کاری: `10`
- امضای هر دو خوراک موجود: معتبر
- هیچ خوراکی در ساخت این بسته بازتولید یا منتشر نشده است.

## کنترل امنیت بسته

موارد زیر در بسته وجود ندارند:

- `*.pem`
- `*.key`
- `*.jks`
- `*.keystore`
- `key.properties`
- `.env`
- APK
- `pubspec.lock`

کلید خصوصی خوراک فقط از Secret با نام `HOLIDAY_FEED_PRIVATE_KEY_B64` دریافت می‌شود.

## محدودیت اعتبارسنجی

Flutter SDK در محیط ساخت محلی حاضر نبود؛ به‌دلیل تغییرنکردن بایت‌به‌بایت سورس Flutter/Android، Test و Analyze برنامه در این مرحله تکرار نشد. اجرای زنده `dry_run=true`، اجرای واقعی GitHub Actions و دریافت خوراک روی گوشی هنوز انجام نشده‌اند. نهایی‌بودن ربات فقط پس از Push روی Branch جدا، اجرای Live dry-run، بررسی Artifact و سپس اجرای واقعی قابل اعلام است.
