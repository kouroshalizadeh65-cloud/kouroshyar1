# گزارش اعتبارسنجی سورس v3.6.45+117

## مبنا

- سورس پایه: `kouroshyar_v3_6_44_finance_labels_hotfix_1405_04_19.zip`
- نسخه خروجی: `3.6.45+117`
- خروجی: سورس کامل Flutter/Android؛ APK در این محیط ساخته نشده است.

## کنترل‌های ایستا

- تجزیه نحوی همه فایل‌های Dart با parser مستقل.
- کنترل صحت XMLهای Android.
- کنترل ساختار YAML گردش‌کار GitHub Actions.
- کنترل وجود همه importهای نسبی داخلی.
- کنترل نسخه `pubspec.yaml` و نام Artifact.
- کنترل وجود پوشه Android، Manifest، MainActivity، Gradle و منابع.
- کنترل نبود keystore، `key.properties`، رمز یا Secret داخل سورس.
- کنترل سلامت و نبود مسیر مشکوک در ZIP نهایی.

## موارد وابسته به GitHub Actions

موارد زیر فقط پس از اجرای workflow قابل تأیید قطعی‌اند:

- `flutter pub get`
- `flutter test`
- `flutter analyze`
- `flutter build apk --release`
- صحت امضای APK با Secrets دائمی
- یکسان‌بودن fingerprint گواهی با نسخه نصب‌شده
- نصب مستقیم APK روی نسخه قبلی

## محدودیت‌های شناخته‌شده این نسخه

- فایل پشتیبان کامل است و SHA-256 دارد، اما رمزگذاری مستقل ندارد.
- اعلان زمان‌بندی‌شده واقعی هنوز پیاده‌سازی نشده و سرویس اعلان فعلی خالی است.
- `pubspec.lock` به‌دلیل نبود Flutter/Dart محلی تولید نشده است؛ workflow در نخستین Build وابستگی‌ها را resolve می‌کند.
- پردازش پشتیبان‌ها و PDF/تصاویر بسیار بزرگ ممکن است حافظه قابل‌توجهی مصرف کند و باید روی گوشی واقعی تست شود.
