# گزارش اعتبارسنجی سورس v3.6.55+127

- مبنا: `v3.6.54+126`.
- نسخه `3.6.55+127` در `pubspec.yaml` ثبت شده است.
- Package ID و namespace برابر `com.example.kouroshyar` باقی مانده است.
- Workflow ساخت، `flutter test`، `flutter analyze` و `flutter build apk --release` را اجرا می‌کند.
- نام APK و Artifact در Workflow به نسخه 3.6.55 به‌روزرسانی شده است.
- فایل `pubspec.lock` داخل سورس تولید نشده است و فقط با اجرای واقعی `flutter pub get` ساخته می‌شود.
- هیچ فایل keystore، کلید خصوصی، `key.properties` یا رمز امضا در سورس وجود ندارد.
- ساعت کاری دوره‌ای از تغییرات موردی جدا شده و در نمای ماه و روز فقط به‌صورت متن ساده نمایش داده می‌شود.
- تعطیلی‌های رسمی موردی ۱۴ و ۱۵ تیر ۱۴۰۵ در داده آفلاین ثبت شده‌اند.

این گزارش جایگزین اجرای واقعی GitHub Actions، بررسی امضا و تست نصب روی گوشی نیست.
